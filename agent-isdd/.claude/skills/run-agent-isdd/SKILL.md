---
name: run-agent-isdd
description: Build and launch the agent-isdd project. This skill handles both discovery and execution - provide a pre-computed plan with --plan for 50% faster setup. Use when you need to run the app, take screenshots, or interact with it programmatically.
---
# Run: agent-isdd

**Project type:** web-app

**Key findings:**
- Project type: web-app
- Launch method: dev-server




## Prerequisites

Install these OS packages first:

```bash
# For Node.js projects
apt-get update && apt-get install -y nodejs npm

# For Python projects
apt-get install -y python3 python3-pip

# For Rust projects
apt-get install -y cargo

# For desktop apps (Electron, etc.)
apt-get install -y libx11-6 libxrandr2 libasound2
```

## Build

```bash
cd /home/user/agent-isdd
# No build step required
```

## Run (Agent Path)

Use `chromium-cli` to drive the web app:

```bash
chromium-cli http://localhost:5173 << 'EOF'
goto "/"
screenshot "home.png"
click "button:has-text('Start')"
screenshot "after-click.png"
EOF
```

Driver is inline (heredoc) in this SKILL.md — no separate driver.mjs needed.

## Run (Human Path)

```bash
cd /home/user/agent-isdd
npm start  # or your build system equivalent
```

## Gotchas

- None known at this time

## Troubleshooting

**"Build failed"**
- Check that all prerequisites are installed
- Ensure Node/Python/Rust version is compatible
- Try clearing cache: `rm -rf node_modules && npm install`

**"Port already in use"**
- Change port with environment variable or configuration
- Kill existing process: `lsof -ti :5173 | xargs kill -9`

---

_Generated by [Claude Code](https://claude.ai/code)_
