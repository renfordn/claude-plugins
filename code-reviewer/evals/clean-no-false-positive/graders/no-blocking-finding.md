---
type: llm
---

The file under review is a correct ASCII slugify function with no bugs.
PASS if the response reports no finding of `critical` or `high` severity and nothing it says
should block the change. Low/nit observations (e.g. non-ASCII input handling) are fine.
FAIL if it reports any critical/high or blocking finding.
