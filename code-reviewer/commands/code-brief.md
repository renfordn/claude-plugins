---
description: Explain how existing code works — a visual Artifact walkthrough, not a review
argument-hint: [file, feature, or subsystem to explain]
---

Use the `code-reviewer:code-reviewer` skill in `research-brief` mode (see SKILL.md's "Research
Brief Output" section) to explain, not review, the following:

$ARGUMENTS

This is not a code review — no `ReportFindings` call, no Decision Model fields, nothing written
to review state. The output is a single Artifact: a structure and/or timeline diagram plus a
narrative walkthrough, grounded in the Evidence Tier Model (tier-1..5) with an "Unverified areas"
section for anything below tier-3.

If no scope was given above, ask one question — what to explain — rather than guessing at a
subsystem.
