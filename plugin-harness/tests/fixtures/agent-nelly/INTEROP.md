# agent-nelly INTEROP

> **Test fixture note:** minimal parser test double — exercises capability-field-presence parsing only, and is not kept in sync with the real INTEROP.md contract's field names or structure.

Memory and lesson learning (soft dependency).

This plugin is marked as a soft dependency and may not always be available in the workflow.

## Capabilities

### memory_brief

Retrieve project memory and error lessons.

Produces:
- memory_context: object
- learned_patterns: array
- error_lessons: array
